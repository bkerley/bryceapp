<p>
  <%= @tweet.body %>
</p>
<p>
  <%= time_ago_in_words @tweet.created_at %> ago
  /
  <%= @tweet.created_at.localtime.to_formatted_s(:long_ordinal) %>
</p>

<%= link_to 'Edit', edit_tweet_path(@tweet) %> |
<%= link_to 'Back', tweets_path %>
